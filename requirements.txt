# Core ML / CV
torch>=2.0
numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
scikit-learn>=1.3
matplotlib>=3.7

# CLIP (OpenAI) — install from GitHub
# pip install git+https://github.com/openai/CLIP.git
clip @ git+https://github.com/openai/CLIP.git

# HTTP (used by video_generator)
requests>=2.31
